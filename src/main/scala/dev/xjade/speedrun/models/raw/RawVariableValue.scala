package dev.xjade.speedrun.models.raw

case class RawVariableValue(
                             label: String,
                             rules: String,
                             flags: RawVariableFlags
                           )
