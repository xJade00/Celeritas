package dev.xjade.speedrun.models.raw

case class RawStatus(
                      status: String,
                      examiner: String,
                      `verify-date`: String
                    )
